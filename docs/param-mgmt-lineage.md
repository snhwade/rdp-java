# 参数管理优化定稿与待办

> 更新：2026-08-23  
> 状态：Q1-B 已落地；其余项待确认/待做

---

## 已确认原则

1. **字段变更受血缘约束**：删除 / 修改字段 code 时，有引用则**阻断**，并提示引用方。  
2. **不做预置业务场景模板**：各企业场景不同，平台不提供行业模板；接入规范另议（平台步骤/启用门禁），本期未做。

---

## Q1 字段血缘（本期）

| 项 | 结论 |
|----|------|
| 范围 | **B**：事件字段绑定、规则包（`rule_v2`）、决策流、指标 |
| 阻断动作 | 删除字段；修改字段 **code** |
| 允许 | 改展示名（name）等非身份字段（有引用时仍允许） |
| 错误码 | `FIELD.IN_USE` |
| 控制台 | 字段库支持删除；关联关系展示「血缘引用」；有引用时删除失败并提示 |

### 实现位置

- `FieldReferenceChecker` / `FieldReferenceCheckerConfig`（rule-config-service）
- `FieldService.deleteField` / `updateField`（改 code）
- `admin-console` 字段库页删除 + 关联关系展示

---

## 待办：Q1-C（文档任务，未做）

**目标：** 在 B 基础上扩展血缘扫描范围。

建议追加来源（实现时再定精确表/字段）：

- [ ] Agent 策略 `config_json` 中的 `featureFields` / 工具 refs
- [ ] 决策表 / 决策树 / 决策矩阵中的字段引用
- [ ] 评级模型条件表达式（若未已被规则侧覆盖）
- [ ] 验证策略或其它 JSON 配置中的字段 code

验收：删除/改 code 时，上述任一存在引用即 `FIELD.IN_USE`，并列出对应类型名。

---

## 搁置（未定稿，不在本期）

| ID | 主题 | 说明 |
|----|------|------|
| Q2 | 接入工作流 / 启用预检 | 不做业务模板；是否做「平台步骤 + 启用门禁」待后续问答 |
| Q3 | 验证策略生效态可视 | 待后续问答 |

---

## 下一步（产品问答）

参数管理 Q1 已定并落地 B；确认进入 **模块 2：规则管理** 的问题与方案问答。
